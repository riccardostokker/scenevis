# scene-analyzer

Follow the repository guidance in `AGENTS.md`.

- Use `mise run check` for fast read-only validation and `mise run test` for tests.
- Manage dependencies and the committed lockfile with uv.
- Keep Python code typed and organized under `src/scene_analyzer/`.
- Use Conventional Commits; git-cliff owns changelog and version calculation.
